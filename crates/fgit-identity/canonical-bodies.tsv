# fgit-canonical-body-descriptions-v1
# family<TAB>canonical payload description
deploy-key-binding	Repository and principal identities, signature-scheme tag, fixed public-key bytes, and the granted canonical scope set for one deploy-key binding.
token-grant	Token handle, principal and repository identities, audience bytes, canonical operation set, budget, and expiry logical time.
