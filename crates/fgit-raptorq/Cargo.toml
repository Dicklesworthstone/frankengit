[package]
name = "fgit-raptorq"
version = "0.1.0"
edition.workspace = true
publish = false

[dependencies]
asupersync = { version = "0.4.9", default-features = false }
fgit-crypto.workspace = true
fgit-object-fabric.workspace = true
fgit-resource.workspace = true
fgit-types.workspace = true

[dev-dependencies]
# FG-024b: the destructive drill emits a genuinely SIGNED reconstruction report
# -- Ed25519 detached envelope over a domain-separated evidence body -- rather
# than a report that is "signed-format" in name only. Test tree only; this
# crate's production code takes no crypto dependency.
fgit-crypto.workspace = true

[lints]
workspace = true
