#![forbid(unsafe_code)]

/// Real fixture target so the table-form dependency syntax is exercised in a
/// complete workspace member rather than an isolated manifest fragment.
pub struct TableHeaderFixture;
