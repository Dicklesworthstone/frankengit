#![forbid(unsafe_code)]

#[allow(clippy::too_many_arguments)]
pub fn widened_surface(
    one: u8,
    two: u8,
    three: u8,
    four: u8,
    five: u8,
    six: u8,
    seven: u8,
    eight: u8,
) -> u8 {
    one + two + three + four + five + six + seven + eight
}

