name: Native branch verification
on:
  push:
    branches: [main]
    paths:
      - .github/workflows/source-snapshot.yml
      - crates/fgit-node/src/treefs_workspace/branches/**
      - crates/fgit-node/src/treefs_workspace/branches.rs
      - crates/fgit-cli/src/branches/**
      - crates/fgit-cli/src/branches.rs
      - crates/fgit-cli/tests/native_branch_smoke.rs
      - scripts/e2e/branch_smoke.py
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-22.04
    timeout-minutes: 25
    env:
      RCH_CARGO_WRAPPER_BYPASS: '1'
      CARGO_BUILD_JOBS: '2'
      CARGO_TARGET_DIR: /tmp/fg-branch-native
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Execute node and CLI including fresh-process branch coverage
        shell: bash
        run: |
          set -euo pipefail
          out="$RUNNER_TEMP/fg-branch-evidence"
          mkdir -p "$out"
          git rev-parse HEAD | tee "$out/revision.txt"
          rustc -Vv | tee "$out/rustc.txt"
          python3 -m py_compile scripts/e2e/branch_smoke.py
          git diff --check
          set +e
          cargo test --locked -p fgit-node -p fgit-cli --all-targets --no-fail-fast -- --nocapture > "$out/test.log" 2>&1
          code=$?
          echo "$code" > "$out/test.exit"
          tail -n 180 "$out/test.log"
          exit "$code"
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        if: always()
        with:
          name: branch-native-${{ github.sha }}
          path: ${{ runner.temp }}/fg-branch-evidence
          if-no-files-found: error
          retention-days: 1
