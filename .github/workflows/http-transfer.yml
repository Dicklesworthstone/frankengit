name: HTTP continuation publish
on:
  push:
    branches: [tooling/http-transfer-c15bea04]
    paths:
      - .github/workflows/http-transfer.yml
      - tools/http-transfer/publish-continuation.sh
permissions:
  contents: write
jobs:
  publish:
    runs-on: ubuntu-22.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 0
          persist-credentials: true
      - name: Reconstruct, format, verify, and fast-forward main
        run: bash tools/http-transfer/publish-continuation.sh
